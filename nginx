docker-compose.yaml

services:
  nginx-proxy:
    image: nginx:latest
    container_name: nginx-reverse-proxy
    restart: always
    ports:
      - "80:80"
      - "8443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /dev/null:/etc/nginx/conf.d/default.conf:ro
      - ./nginx_path:/usr/share/nginx/html:rw




nginx.conf
events {
    worker_connections 1024;
}

http {
    # Redirecionar HTTP para HTTPS
    server {
        listen 80;
        server_name www.carlosdomingues.pt carlosdomingues.pt;
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl;
        server_name www.carlosdomingues.pt carlosdomingues.pt;

            add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    add_header Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=(), usb=()" always;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

    add_header Cross-Origin-Resource-Policy "same-origin" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    #add_header Cross-Origin-Embedder-Policy "require-corp" always
    add_header Cross-Origin-Embedder-Policy: unsafe-none;

    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https:; style-src 'self' 'unsafe-inline' https:; img-src 'self' data: https:; font-src 'self' https: data:; connect-src 'self' https:; frame-ancestors 'self'; base-uri 'self'; form-action 'self'" always;



        # Configuração SSL (Caminhos internos do container)
        ssl_certificate /etc/letsencrypt/live/carlosdomingues.pt/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/carlosdomingues.pt/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Serviço 1 (Exemplo: uma API ou App na porta 8080)
        location /servico1/ {
            proxy_pass http://172.17.0.1:8081/; # IP do Host no Docker
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        # Serviço 2 (Exemplo: um Dashboard na porta 9000)
        location /juicy-shop-x34534534/ {
            proxy_pass http://172.17.0.1:3000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
        # Serviço N8N
        location /n8n/ {
            proxy_pass http://138.197.184.220:5678;
            proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';

        }


        location / {
            proxy_pass http://138.197.184.220:3000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }


        # Root ou Página Principal
        location /carlos {
            root /usr/share/nginx/html;
            server_tokens off;
            # Security Headers
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    add_header Permissions-Policy "geolocation=(), microphone=(), camera=(), payment=(), usb=()" always;

    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

    add_header Cross-Origin-Resource-Policy "same-origin" always;
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Cross-Origin-Embedder-Policy "require-corp" always;

    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' https:; style-src 'self' 'unsafe-inline' https:; img-src 'self' data: https:; font-src 'self' https: data:; connect-src 'self' https:; frame-ancestors 'self'; base-uri 'self'; form-action 'self'" always;

            index index.html;
        }
    }
   server {
        listen 443 ssl;
        server_name banco.carlosdomingues.pt;

        # Configuração SSL (Caminhos internos do container)
        ssl_certificate /etc/letsencrypt/live/carlosdomingues.pt/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/carlosdomingues.pt/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Serviço 1 (Exemplo: uma API ou App na porta 8080)
        location / {
            proxy_pass http://10.0.0.31:5001/; # IP do Host no Docker
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

    }

   server {
        listen 443 ssl;
        server_name app1.carlosdomingues.pt;

        # Configuração SSL (Caminhos internos do container)
        ssl_certificate /etc/letsencrypt/live/carlosdomingues.pt/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/carlosdomingues.pt/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Serviço 1 (Exemplo: uma API ou App na porta 8080)
        location / {
            proxy_pass http://10.0.0.31:81/; # IP do Host no Docker
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

    }


     server {
        listen 443 ssl;
        server_name admin.carlosdomingues.pt;

        # Configuração SSL (Caminhos internos do container)
        ssl_certificate /etc/letsencrypt/live/www.carlosdomingues.pt/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/www.carlosdomingues.pt/privkey.pem;

        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;

        # Serviço 1 (Exemplo: uma API ou App na porta 8080)
        location / {
            proxy_pass http://138.197.184.220:5678; # IP do Host no Docker
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

    }


}
